import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cn.jiuyivault.app',
  appName: '九一集英社',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
